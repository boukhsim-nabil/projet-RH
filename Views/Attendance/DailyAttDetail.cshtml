@model List<supmtigroupe.Models.hrms.tempMonthAttModel>
@{
    ViewData["Title"] = "Présence Journalière";
    ViewData["active"] = "DATT";
}


<!-- Page Header -->
<div class="page-header">
    <div class="row">
        <div class="col-sm-12">
            <h3 class="page-title">Présence Journalière </h3>
            <ul class="breadcrumb">
                <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">Tableau de Bord</a></li>
                <li class="breadcrumb-item active">Présence Journalière </li>
            </ul>
        </div>
    </div>
</div>
<!-- /Page Header -->
<div class="row">
    <div class="col-md-12">
        <div class="table-responsive">
            <table id="example4" class="table table-striped custom-table leave-employee-table mb-0">
                <thead>
                    <tr>
                        <th>N°</th>
                        <th>Département</th>
                        <th>Employé</th>
                        <th>Entrée</th>
                        <th>Retard</th>
                        <th>Absent</th>
                        <th>Présent</th>
                        <th>Congé</th>
                        <!-- Add other table headers here -->
                    </tr>
                </thead>
                <tbody>

                    @for (int i = 0; i < Model.Count; i++)
                    {
                        <tr>
                            <td>@(i + 1)</td>
                            <td>@Model[i].emp.depart.name</td>
                            <td>
                                <h2 class="table-avatar">
                                    <a href="#" class="avatar"><img src="~/dist/files/@Model[i].emp.profile_pic" alt="User Image"></a>
                                    <a href="#">@Model[i].emp.name</a>
                                </h2>
                            </td>
                            <td>@Model[i].Checkin.TimeOfDay</td>
                            <td>@(Model[i].late ? "?" : "")</td>
                            <td>@(Model[i].absent ? "?" : "")</td>
                            <td>@(Model[i].present ? "?" : "")</td>
                            <td>@Model[i].leave</td>
                        </tr>
                    }
                </tbody>

            </table>
        </div>
    </div>
</div>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
